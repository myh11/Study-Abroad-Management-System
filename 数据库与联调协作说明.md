# 数据库与联调协作说明

本文档用于统一本地开发、CI、联调、测试数据和开发边界。

## 1. 数据库运行配置

后端项目路径：

- `D:\大二下\软件开发与设计\实践二\admission-system-backend`

当前后端配置文件：

- [application.yml](D:/大二下/软件开发与设计/实践二/admission-system-backend/src/main/resources/application.yml:1)

当前默认运行参数：

- 服务端口：`8080`
- 数据库主机：`localhost`
- 数据库端口：`3306`
- 数据库名：`admission_system`
- 数据库用户：`root`
- 数据库密码：`mYh031005`

当前配置方式：

- `SERVER_PORT`
- `DB_HOST`
- `DB_PORT`
- `DB_NAME`
- `DB_USERNAME`
- `DB_PASSWORD`

说明：

1. `application.yml` 现在使用“环境变量 + 默认值”模式。
2. 本地不配环境变量时，默认走：
   - `8080`
   - `localhost:3306`
   - `admission_system`
   - `root / mYh031005`
3. CI 已补同名环境变量，配置文件和 CI 读取同一套键，避免本地和流水线出现两份不同数据库参数。

CI 文件：

- [backend-ci.yml](D:/大二下/软件开发与设计/实践二/admission-system-backend/.github/workflows/backend-ci.yml:1)

## 2. 测试账号清单

初始化账号来自：

- [data.sql](D:/大二下/软件开发与设计/实践二/admission-system-backend/src/main/resources/data.sql:1)

固定账号如下：

| 账号 | 密码 | 角色 | 归属学校 |
| --- | --- | --- | --- |
| `admin` | `admin123` | `ADMIN` | 无 |
| `agent01` | `agent123` | `AGENT` | 无 |
| `domestic01` | `domestic123` | `DOMESTIC_REVIEWER` | 无 |
| `school_usyd_01` | `school123` | `SCHOOL_REVIEWER` | `USYD` |
| `school_anu_01` | `school123` | `SCHOOL_REVIEWER` | `ANU` |
| `school_unsw_01` | `school123` | `SCHOOL_REVIEWER` | `UNSW` |

补充说明：

1. 当前初始化账号默认 `must_change_password = true`。
2. 首次登录后应先调用 `/api/auth/change-password`。
3. 学校审核员只能处理自己 `school_code` 归属学校的申请和候补数据。

## 3. 固定联调数据说明

当前固定联调数据主要在：

- [data.sql](D:/大二下/软件开发与设计/实践二/admission-system-backend/src/main/resources/data.sql:428)

建议直接使用以下申请 ID 进行联调：

| 申请 ID | 当前状态 | 建议用途 |
| --- | --- | --- |
| `124` | `DOMESTIC_SUPPLEMENT` | 补件场景：上传新成绩单后再重新提交 |
| `127` | `WAITLISTED` | 候补场景：查看候补详情、学校推进候补 |
| `128` | `WAITLIST_PENDING_CONFIRM` | 候补确认场景：代理接受或拒绝 |
| `129` | `RESERVED` | 预录取终态展示 |
| `130` | `ADJUSTMENT_SUGGESTED` | 调剂建议场景：代理拒绝调剂 |
| `132` | `CANCELED` | 撤销终态展示与“不可同批次再申同校”验证 |
| `137` | `WAITLISTED` | 第二组候补样本，便于并行联调 |
| `138` | `RESERVED` | 第二组预录取样本 |
| `142` | `DOMESTIC_SUPPLEMENT` | 第二组补件样本 |
| `144` | `ADJUSTMENT_SUGGESTED` | 第二组调剂建议样本 |

补充说明：

1. 如果要验证“学校审核 -> 候补 -> 推进 -> 代理确认”，可以使用 `127` 或 `137`。
2. 如果要验证“学校审核 -> 调剂建议 -> 生成调剂草稿 -> 代理接受调剂”，建议从学校审核接口重新制造数据，不建议直接改库。
3. 如果要验证“批次结束后候补自动失效”，应通过调整批次结束时间或批次状态触发，不要直接改 `applications.current_status`。

## 4. 接口调用顺序说明

建议前端或测试按以下顺序调用核心流程接口。

### 4.1 登录与初始化

1. `POST /api/auth/login`
2. 如返回 `mustChangePassword = true`：
   - `POST /api/auth/change-password`
3. `GET /api/auth/me`

### 4.2 代理标准申请流程

1. `POST /api/files/transcripts`
2. `POST /api/applications`
3. `GET /api/applications/my`
4. `GET /api/applications/{id}`
5. `POST /api/applications/{id}/submit`

### 4.3 国内审核流程

1. `POST /api/domesticreviews/{id}/claim`
2. `POST /api/domesticreviews/{id}/submit`

提交结果分三类：

- 通过：进入 `SCHOOL_REVIEWING`
- 补件：进入 `DOMESTIC_SUPPLEMENT`
- 拒绝：进入 `DOMESTIC_REJECTED`

### 4.4 补件流程

1. `POST /api/files/transcripts`
2. `POST /api/applications/{id}/supplement`
3. `POST /api/applications/{id}/submit`

说明：

1. `supplement` 只替换补件文件，不自动改回 `SUBMITTED`
2. 真正重新进入审核，要再调一次 `submit`

### 4.5 学校审核流程

1. `GET /api/applications/{id}`
2. `POST /api/school-reviews/{id}/submit`

提交结果分四类：

- `RESERVE`
- `WAITLIST`
- `REJECT`
- `SUGGEST_ADJUSTMENT`

### 4.6 候补流程

1. `GET /api/waitlists/{applicationId}`
2. `POST /api/waitlists/{applicationId}/promote`
3. 代理侧：
   - `POST /api/applications/{id}/waitlist-confirm`

说明：

1. `promote` 后状态会进入 `WAITLIST_PENDING_CONFIRM`
2. 接受后进入 `RESERVED`
3. 拒绝后进入 `CLOSED`
4. 超时或批次结束会自动关闭候补

### 4.7 调剂流程

学校侧：

1. `POST /api/school-reviews/{id}/submit`

代理侧：

1. `GET /api/applications/{id}`
2. 接受调剂：
   - `POST /api/applications/{id}/accept-adjustment`
3. 拒绝调剂：
   - `POST /api/applications/{id}/reject-adjustment`

### 4.8 管理端配置流程

建议顺序：

1. 批次
2. 学校
3. 专业
4. 学校总名额
5. 专业名额
6. 用户

说明：

1. 已发布批次下，学校总名额必须等于该校所有专业名额之和。
2. 不一致时会阻止发布，也会阻止把已发布批次改成不一致配置。

## 5. 开发禁区说明

以下字段只能通过业务接口或业务 service 修改，不能通过前端随意传值直接改库。

### 5.1 申请状态相关

涉及表：

- `applications`
- `application_status_histories`
- `audit_logs`
- `waitlist_records`

禁止直接改的关键字段：

- `applications.current_status`
- `applications.cancel_reason`
- `applications.close_reason`
- `applications.rule_snapshot_id`
- `applications.waitlist_confirm_deadline`
- `applications.reserved_at`
- `applications.canceled_at`
- `applications.closed_at`
- `waitlist_records.waitlist_status`
- `waitlist_records.promoted_at`
- `waitlist_records.confirm_deadline`
- `waitlist_records.expired_at`

原因：

1. 这些字段必须通过统一状态机入口修改。
2. 修改时要同步写入：
   - `application_status_histories`
   - `audit_logs`
3. 直接改库会破坏状态流、审计链和联调结果。

### 5.2 配额相关

涉及表：

- `school_quotas`
- `major_quotas`

禁止直接改的关键字段：

- `total_quota`
- `used_quota`
- `remaining_quota`
- `quota_version`

原因：

1. 配额占用/释放由业务流程驱动。
2. 直接改 `used_quota` 或 `remaining_quota` 会破坏并发控制和候补/预录取链路。
3. 配额调整应走管理端接口，不要绕过 service 层。

### 5.3 用户与安全相关

涉及表：

- `users`

禁止直接改的关键字段：

- `password_hash`
- `must_change_password`
- `login_fail_count`
- `locked_until`
- `role_type`
- `school_code`

原因：

1. 这些字段涉及登录安全、首次改密、连续输错锁定、RBAC 和学校归属控制。
2. 必须通过认证逻辑或管理端逻辑改动。

### 5.4 文件与规则快照相关

涉及表：

- `files`
- `rule_snapshots`

禁止直接改的关键字段：

- `files.storage_path`
- `files.uploaded_by`
- `rule_snapshots.school_threshold_snapshot`
- `rule_snapshots.major_threshold_snapshot`
- `rule_snapshots.reserve_line_snapshot`
- `rule_snapshots.waitlist_line_snapshot`
- `rule_snapshots.quota_rule_version`

原因：

1. 文件路径和上传者权限会影响预览/下载鉴权。
2. 规则快照是申请提交时固化的业务依据，不能人工补写或覆盖。

## 6. 协作约定

1. 前端、测试、后端联调时，优先使用固定申请 ID，不要每次手工改库造数。
2. 若要新增联调数据，优先走接口脚本或初始化 SQL，不要直接在运行中修改状态字段。
3. 所有“看似能改”的状态字段，都默认视为禁改字段，除非明确在 service 层提供了业务接口。
4. 如果联调结果和数据库状态不一致，先查：
   - `application_status_histories`
   - `audit_logs`
5. 数据口径冲突时，以代码实际实现和本说明为准，再回写到接口文档。
