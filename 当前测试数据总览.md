# 当前测试数据总览

文档优先级：`PRD > 后端接口文档 v4 > 数据库与联调协作说明 > 当前测试数据总览 > 个人理解`

适用范围：当前本地库 `admission_system`，初始化来源为 [schema.sql](D:/大二下/软件开发与设计/实践二/admission-system-backend/src/main/resources/schema.sql:1) 与 [data.sql](D:/大二下/软件开发与设计/实践二/admission-system-backend/src/main/resources/data.sql:1)。

## 一、这份文档解决什么问题

- 这份文档用于集中说明“当前测试数据到底有什么、每组数据现在是什么情况、应该拿来做什么”。
- 现有文档里已经分别写了：
  - 测试账号
  - 固定联调 `applicationId`
  - 数据库协作规则
- 但此前没有一份文档把这些信息集中成“数据地图”。
- 从现在开始，联调和演示优先看这份文档，不要靠翻 `data.sql` 猜。

## 二、当前数据由哪些部分组成

当前初始化数据主要分为 7 类：

1. 用户账号数据
2. 批次数据
3. 学校与专业主数据
4. 学校总名额与专业名额数据
5. 学生、成绩单、个人陈述、文件元数据
6. 申请主表数据
7. 审核、候补、状态历史、审计日志

## 三、当前测试账号总表

| 账号 | 密码 | 角色 | 学校归属 | 用途 |
| --- | --- | --- | --- | --- |
| `admin` | `admin123` | `ADMIN` | 无 | 管理端、日志、配置、全局排查 |
| `agent01` | `agent123` | `AGENT` | 无 | 代理端申请、补件、撤销、候补确认、调剂 |
| `domestic01` | `domestic123` | `DOMESTIC_REVIEWER` | 无 | 国内审核认领、提交 |
| `school_usyd_01` | `school123` | `SCHOOL_REVIEWER` | `USYD` | 只处理 `USYD` 学校审核与候补 |
| `school_anu_01` | `school123` | `SCHOOL_REVIEWER` | `ANU` | 只处理 `ANU` 学校审核与候补 |
| `school_unsw_01` | `school123` | `SCHOOL_REVIEWER` | `UNSW` | 只处理 `UNSW` 学校审核与候补 |

强制说明：
- 所有初始化账号默认 `must_change_password = true`
- 首次登录后必须先改密
- 学校审核员只能访问自己学校的数据

## 四、当前主数据总览

### 1. 批次

| batchId | 批次名 | 状态 | 用途 |
| --- | --- | --- | --- |
| `1` | `2026 Fall Batch` | `IN_PROGRESS` | 当前主要联调批次 |
| `2` | `2025 Fall Batch` | `FINISHED` | 已结束批次，用于历史样本和关闭样本 |

### 2. 学校

| schoolCode | 学校名 |
| --- | --- |
| `ANU` | Australian National University |
| `USYD` | University of Sydney |
| `UNSW` | University of New South Wales |

### 3. 专业

| majorCode | schoolCode | 专业名 | allowAdjustmentIn |
| --- | --- | --- | --- |
| `ANU_CS` | `ANU` | Computer Science | `0` |
| `ANU_IR` | `ANU` | International Relations | `1` |
| `USYD_SE` | `USYD` | Software Engineering | `0` |
| `USYD_BIZ` | `USYD` | Business | `1` |
| `UNSW_DS` | `UNSW` | Data Science | `0` |
| `UNSW_ME` | `UNSW` | Mechanical Engineering | `0` |

说明：
- `allowAdjustmentIn = 1` 表示当前初始化规则允许作为调剂建议目标专业。
- 当前具备明显调剂语义的专业主要是：
  - `ANU_IR`
  - `USYD_BIZ`

## 五、当前配额数据概况

### 1. 当前主联调批次：`batchId = 1`

| schoolCode | school total | school used | school remaining | 说明 |
| --- | --- | --- | --- | --- |
| `ANU` | `30` | `28` | `2` | 仍有学校总名额余量 |
| `USYD` | `35` | `34` | `1` | 学校总名额接近满额 |
| `UNSW` | `32` | `32` | `0` | 学校总名额已满，适合候补/调剂场景 |

### 2. 当前主联调批次主要专业余量

| majorCode | total | used | remaining | 说明 |
| --- | --- | --- | --- | --- |
| `ANU_CS` | `15` | `13` | `2` | 可做正常学校审核/录取样本 |
| `ANU_IR` | `15` | `14` | `1` | 易进入边界候补 |
| `USYD_SE` | `18` | `17` | `1` | 适合预录取/候补边界样本 |
| `USYD_BIZ` | `17` | `16` | `1` | 适合候补/撤销/调剂联调 |
| `UNSW_DS` | `16` | `16` | `0` | 已满，适合候补和调剂建议 |
| `UNSW_ME` | `16` | `15` | `1` | 适合拒绝/边界场景 |

强制说明：
- 配额字段只能看，不能手改
- 配额变化必须通过学校审核、候补、管理端接口驱动

## 六、当前申请样本总表

### 1. 主样本（推荐联调）

| applicationId | batchId | 学校 / 专业 | 当前状态 | 当前情况 | 推荐用途 |
| --- | --- | --- | --- | --- | --- |
| `124` | `1` | `ANU / ANU_IR` | `DOMESTIC_SUPPLEMENT` | 国内审核要求补件，尚未重新提交 | 补件上传 + 重新提交 |
| `127` | `1` | `USYD / USYD_BIZ` | `WAITLISTED` | 已进入候补，候补记录为 `ACTIVE` | 候补详情 + 学校推进候补 |
| `128` | `1` | `UNSW / UNSW_DS` | `WAITLIST_PENDING_CONFIRM` | 已从候补推进，等待代理确认 | 候补确认接受/拒绝 |
| `129` | `1` | `USYD / USYD_SE` | `RESERVED` | 已预录取，且对应候补记录已 `CLOSED` | 预录取终态展示 |
| `130` | `1` | `ANU / ANU_IR` | `ADJUSTMENT_SUGGESTED` | 学校建议调剂，尚未处理 | 调剂建议查看 + 拒绝调剂 |
| `132` | `1` | `USYD / USYD_BIZ` | `CANCELED` | 代理已撤销 | 撤销终态展示 + 同批次不可再申同校验证 |
| `137` | `1` | `ANU / ANU_IR` | `WAITLISTED` | 第二组候补样本，候补记录为 `ACTIVE` | 候补并行联调 |
| `138` | `1` | `UNSW / UNSW_DS` | `RESERVED` | 第二组预录取样本，候补记录已关闭 | 预录取并行展示 |
| `142` | `1` | `USYD / USYD_BIZ` | `DOMESTIC_SUPPLEMENT` | 第二组补件样本 | 补件并行联调 |
| `144` | `1` | `UNSW / UNSW_DS` | `ADJUSTMENT_SUGGESTED` | 第二组调剂建议样本 | 调剂建议并行联调 |

### 2. 其他可参考样本

| applicationId | 当前状态 | 说明 |
| --- | --- | --- |
| `121` | `DRAFT` | 草稿样本 |
| `122` | `SUBMITTED` | 已提交待国内审核样本 |
| `123` | `DOMESTIC_REVIEWING` | 国内审核中样本 |
| `125` | `DOMESTIC_REJECTED` | 国内审核拒绝样本 |
| `126` | `SCHOOL_REVIEWING` | 学校审核中样本 |
| `131` | `SCHOOL_REJECTED` | 学校拒绝样本 |
| `133` | `CLOSED` | 历史批次关闭样本 |
| `134` | `SUBMITTED` | 第二组已提交样本 |
| `135` | `CLOSED` | 候补超时关闭样本 |
| `136` | `DOMESTIC_REVIEWING` | 第二组国内审核中样本 |
| `139` | `CLOSED` | 原申请已因接受调剂关闭 |
| `140` | `DRAFT` | 调剂接受后生成的新草稿样本，`source_application_id = 139` |
| `141` | `WAITLISTED` | 边界候补样本，不作为首选固定联调 ID |
| `143` | `DOMESTIC_REJECTED` | 边界拒绝样本 |

## 七、当前申请状态样本分布

| 状态 | 当前样本 ID |
| --- | --- |
| `DRAFT` | `121`, `140` |
| `SUBMITTED` | `122`, `134` |
| `DOMESTIC_REVIEWING` | `123`, `136` |
| `DOMESTIC_SUPPLEMENT` | `124`, `142` |
| `DOMESTIC_REJECTED` | `125`, `143` |
| `SCHOOL_REVIEWING` | `126` |
| `WAITLISTED` | `127`, `137`, `141` |
| `WAITLIST_PENDING_CONFIRM` | `128` |
| `RESERVED` | `129`, `138` |
| `ADJUSTMENT_SUGGESTED` | `130`, `144` |
| `SCHOOL_REJECTED` | `131` |
| `CANCELED` | `132` |
| `CLOSED` | `133`, `135`, `139` |

## 八、当前审核数据是什么情况

### 1. 国内审核样本

- 已存在国内审核记录的代表样本：
  - `123`
  - `124`
  - `136`
  - `141`
  - `142`
  - `143`
  - `144`
- 典型结果：
  - `PASS`：`123`, `136`, `141`, `144`
  - `SUPPLEMENT_REQUIRED`：`124`, `142`
  - `REJECT`：`143`

### 2. 学校审核样本

- 已存在学校审核记录的代表样本：
  - `126`
  - `127`
  - `128`
  - `129`
  - `130`
  - `131`
  - `135`
  - `137`
  - `138`
  - `139`
  - `141`
  - `144`
- 典型结果：
  - `RESERVE`：`126`, `129`, `138`
  - `WAITLIST`：`127`, `128`, `135`, `137`, `141`
  - `SUGGEST_ADJUSTMENT`：`130`, `139`, `144`
  - `REJECT`：`131`

## 九、当前候补数据是什么情况

| applicationId | waitlist_status | 当前申请状态 | 当前情况 |
| --- | --- | --- | --- |
| `127` | `ACTIVE` | `WAITLISTED` | 正常候补，尚未推进 |
| `128` | `PROMOTED` | `WAITLIST_PENDING_CONFIRM` | 已推进，等待代理确认 |
| `137` | `ACTIVE` | `WAITLISTED` | 正常候补，第二组样本 |
| `129` | `CLOSED` | `RESERVED` | 候补推进后已确认成功 |
| `135` | `EXPIRED` | `CLOSED` | 候补推进后超时未确认 |
| `138` | `CLOSED` | `RESERVED` | 候补推进后已确认成功 |
| `141` | `ACTIVE` | `WAITLISTED` | 边界候补样本 |

## 十、调剂数据是什么情况

### 1. 推荐使用的调剂样本

- `130`：学校建议调剂，适合代理拒绝调剂
- `144`：第二组学校建议调剂，适合并行联调

### 2. 已经走过“接受调剂”链路的样本

- `139`：原申请已关闭，`close_reason = ADJUSTMENT_ACCEPTED_NEW_APPLICATION_CREATED`
- `140`：由 `139` 派生出的新草稿，`source_application_id = 139`

说明：
- 如果要展示“调剂接受后会生成新申请”，`139 -> 140` 是现成样本
- 如果要演示“从建议调剂到接受调剂”的完整动作，优先重新通过真实学校审核接口制造，不要手改库

## 十一、每类数据应该怎么用

### 1. A（代理端）

优先使用：
- `124`, `142`：补件
- `128`：候补确认
- `130`, `144`：调剂
- `132`：撤销

### 2. B（国内审核）

优先使用：
- `122`, `134`：从 `SUBMITTED` 开始认领
- `123`, `136`：国内审核中展示
- `124`, `142`：补件结果展示
- `143`：拒绝结果展示

### 3. 核心负责人 / 学校审核

优先使用：
- `126`：学校审核中
- `127`, `137`：候补推进
- `128`：候补待确认
- `129`, `138`：预录取
- `130`, `144`：调剂建议

## 十二、哪些数据可以新增，哪些不能手改

### 可以新增

- 新的草稿申请
- 新的上传文件
- 新的演示账号
- 新的固定联调样本

### 新增方式

必须按以下顺序：
1. 优先走真实接口
2. 如需长期复用，再回写 `data.sql`
3. 再补这份文档或协作文档

### 不能手改

- `applications.current_status`
- `school_quotas.used_quota / remaining_quota`
- `major_quotas.used_quota / remaining_quota`
- `waitlist_records.waitlist_status`
- `users.password_hash / must_change_password / login_fail_count / locked_until`

## 十三、数据异常时先看哪里

出现“页面显示和预期不一致”时，按这个顺序查：

1. 查 `application_status_histories`
2. 查 `audit_logs`
3. 查对应审核表：
   - `domestic_reviews`
   - `school_reviews`
   - `waitlist_records`
4. 最后再看 `applications`

## 十四、结论

- 现在已经有一套可直接联调的数据全景：
  - 账号有
  - 主数据有
  - 配额边界有
  - 各状态样本有
  - 候补和调剂闭环样本也有
- A、B、你后续不要再从 `data.sql` 盲猜数据，优先看这份文档和固定联调说明。
