# 数据库协作规则（可执行版）

文档优先级：`PRD > 数据库设计文档 > 数据库与联调协作说明 > 个人理解`

适用范围：`D:\大二下\软件开发与设计\实践二\admission-system-backend`

## 一、本地数据库初始化

- 必须使用 `src/main/resources/schema.sql` 初始化表结构。
- 必须使用 `src/main/resources/data.sql` 初始化基础数据、测试账号、固定联调数据。
- 禁止手动建表；新增表、删表、改字段、改索引，必须先改 SQL 文件，再由负责人统一初始化或迁移。
- 初始化流程必须按以下顺序执行：
  1. 创建空库：`admission_system`
  2. 执行 `schema.sql`
  3. 执行 `data.sql`
  4. 启动后端，校验测试账号可登录
  5. 校验固定联调申请 ID 是否存在
- 本地运行参数必须与协作文档一致：
  - Host：`localhost`
  - Port：`3306`
  - DB：`admission_system`
  - User：`root`
  - Password：`mYh031005`
- CI 与本地必须使用同一套数据库参数键：`DB_HOST / DB_PORT / DB_NAME / DB_USERNAME / DB_PASSWORD`。

## 二、数据库修改规则（强约束）

- 禁止直接改运行库业务数据。
- 必须走 SQL 文件的操作：
  - 建表、删表、改字段、改索引、改约束
  - 初始化账号调整
  - 固定联调数据补充
  - 演示数据脚本补充
- 必须走接口的操作：
  - 登录、改密、锁定相关数据变更
  - 创建申请、补件、提交、撤销、关闭
  - 国内审核、学校审核、候补推进、候补失效、候补确认
  - 调剂接受、调剂拒绝
  - 批次、学校、专业、配额、用户管理
- 禁止通过 Navicat、DataGrip、命令行 SQL 直接修改状态、配额、候补、安全字段。
- 运行库只允许查，不允许手工写；必须写时，只能由负责人在明确修复方案下执行，并同步回写 SQL 或代码。

## 三、关键字段禁区

### 1. `applications.current_status`

- 禁止直接修改。
- 原因：
  - 该字段必须通过统一状态机入口变更。
  - 变更时必须同步写入 `application_status_histories`。
  - 变更时必须同步写入 `audit_logs`。
  - 直接改库会破坏状态链、审计链、联调结论。
- 正确修改方式：
  - 通过申请命令接口、国内审核接口、学校审核接口、候补接口驱动变更。

### 2. `school_quotas / major_quotas`

- 禁止直接修改 `total_quota / used_quota / remaining_quota / quota_version`。
- 原因：
  - 配额占用与释放受业务流程和并发控制约束。
  - 候补推进、候补确认、占位、失效都会联动配额。
  - 直接改 `used_quota` 或 `remaining_quota` 会破坏预占、释放、回滚逻辑。
- 正确修改方式：
  - 通过管理端配额接口调整总量。
  - 通过学校审核、候补流程自动占用/释放配额。

### 3. `waitlist_records`

- 禁止直接修改 `waitlist_status / promoted_at / confirm_deadline / expired_at`。
- 原因：
  - 候补状态必须与 `applications.current_status` 保持一致。
  - 候补推进、失效、批次结束关闭会同步写状态历史和审计日志。
  - 直接改库会导致“申请状态”和“候补记录状态”脱节。
- 正确修改方式：
  - 通过 `/api/waitlists/{applicationId}` 查询
  - 通过 `/api/waitlists/{applicationId}/promote`
  - 通过 `/api/waitlists/{applicationId}/invalidate`
  - 通过代理候补确认接口完成最终状态变更

### 4. `users`

- 禁止直接修改 `password_hash / must_change_password / login_fail_count / locked_until / role_type / school_code`。
- 原因：
  - 这些字段直接影响登录安全、首次改密、连续输错锁定、RBAC、学校归属隔离。
  - 手工修改会导致认证状态与权限状态不一致。
- 正确修改方式：
  - 密码、首次改密、锁定：必须走认证逻辑
  - 角色、学校归属、启停：必须走用户管理接口

## 四、固定联调数据规则

- 必须固定使用以下申请 ID：
  - `124`：`DOMESTIC_SUPPLEMENT`
  - `127`：`WAITLISTED`
  - `128`：`WAITLIST_PENDING_CONFIRM`
  - `129`：`RESERVED`
  - `130`：`ADJUSTMENT_SUGGESTED`
  - `132`：`CANCELED`
  - `137`：`WAITLISTED`
  - `138`：`RESERVED`
  - `142`：`DOMESTIC_SUPPLEMENT`
  - `144`：`ADJUSTMENT_SUGGESTED`
- 候补链路必须优先使用 `127 / 128 / 137`。
- 补件链路必须优先使用 `124 / 142`。
- 调剂链路必须优先使用 `130 / 144`。
- 可以新增的数据：
  - 新的草稿申请
  - 新的测试文件
  - 新的演示账号或演示样本
- 新数据生成方式必须遵守以下顺序：
  1. 优先走真实接口造数
  2. 需要固定复用时，回写到 `data.sql`
  3. 禁止在运行中手工 update 状态字段造数
- 若需新增联调样本，必须由负责人统一分配新 ID 用途，并补充到协作文档。

## 五、表归属

| 表名 | 负责人 | 是否允许他人写入 | 说明 |
| --- | --- | --- | --- |
| `applications` | 你 | 否 | 核心申请表；A/B 只能通过接口 |
| `application_status_histories` | 你 | 否 | 只允许状态机写入 |
| `audit_logs` | 你 | 否 | 只允许后端 service 写入 |
| `rule_snapshots` | 你 | 否 | 只允许提交申请时固化 |
| `waitlist_records` | 你 | 否 | 只允许候补流程写入 |
| `school_quotas` | 你 | 否 | 只允许配额接口和核心流程写入 |
| `major_quotas` | 你 | 否 | 只允许配额接口和核心流程写入 |
| `school_reviews` | 你 | 否 | 只允许学校审核流程写入 |
| `domestic_reviews` | B | 否 | B 负责联调，但只能通过国内审核接口写入 |
| `students` | A | 否 | A 负责联调，但只能通过申请接口写入 |
| `transcripts` | A | 否 | 只能通过上传与补件接口写入 |
| `personal_statements` | A | 否 | 只能通过申请接口写入 |
| `files` | 你 | 否 | 文件元数据只能由上传逻辑写入 |
| `users` | 你 | 否 | 用户与安全字段只能通过认证/管理逻辑修改 |
| `admission_batches` | 你 | 否 | 只能通过批次接口修改 |
| `schools` | 你 | 否 | 只能通过学校接口修改 |
| `majors` | 你 | 否 | 只能通过专业接口修改 |

## 六、事务规则

- 占位必须事务：
  - 必须同时锁申请、锁学校配额、锁专业配额。
  - 必须同时写 `school_reviews`、`applications`、`school_quotas`、`major_quotas`、`application_status_histories`、`audit_logs`。
  - 任一环节失败必须整体回滚。
- 候补补位必须事务：
  - 必须同时更新 `waitlist_records`、`applications`、配额相关表、历史表、日志表。
  - 必须写入待确认截止时间。
  - 任一环节失败必须整体回滚。
- 调剂必须事务：
  - 禁止直接改原申请目标专业。
  - 必须按“原申请状态变更 + 新申请生成/提交 + 历史日志补齐”整体执行。
  - 任一环节失败必须整体回滚。
- 批次结束必须事务化处理候补失效：
  - 必须关闭该批次下候补记录。
  - 必须同步关闭申请候补状态并释放相关占用。
  - 必须补写状态历史和审计日志。

## 七、错误排查流程

当数据异常时，必须按以下顺序排查：

1. 查 `application_status_histories`
   - 先确认状态是否按业务顺序流转
   - 先确认最后一次合法状态是什么
2. 查 `audit_logs`
   - 确认是谁、在什么时间、通过什么操作写入
   - 确认是否存在越权或重复操作
3. 查接口调用链
   - 确认前端调用了哪个接口
   - 确认请求参数是否正确
   - 确认是否跳过了必须的前置动作
4. 最后才允许查运行库现场值
   - 运行库现场只用于确认结果，不作为第一判断依据

## 八、禁止行为

- 禁止直接 `update applications.current_status`。
- 禁止直接改 `school_quotas` 或 `major_quotas` 的配额字段。
- 禁止直接改 `waitlist_records` 状态或时间字段。
- 禁止直接改 `users.password_hash`、`users.must_change_password`、`users.login_fail_count`、`users.locked_until`。
- 禁止跳过 service 层直接从脚本或 SQL 写业务状态。
- 禁止为了联调方便手工造“半流程数据”。
- 禁止新增表、改字段后不回写 `schema.sql`。
- 禁止新增固定联调数据后不回写 `data.sql` 或协作文档。
