# 代理端负责人沟通总结

按《分工启动指南-A》第二十一条格式整理。

---

## 一、我改了哪些页面

- `src/views/applications/ApplicationCreatePage.vue` — 新建申请页（学生信息、成绩单上传、自荐材料、附件上传、申请目标、保存草稿、提交申请）
- `src/views/applications/ApplicationListPage.vue` — 我的申请列表（状态筛选、分页、搜索、快捷提交/撤销/补件）
- `src/views/applications/ApplicationDetailPage.vue` — 申请详情页（信息展示、状态横幅、操作按钮：提交/补件/候补确认/调剂/撤销）

**越界修改（需报备）：**
- `src/views/login/LoginPage.vue` — 新增首次登录强制改密弹窗（后端 PasswordChangeEnforcementFilter 拦截所有请求，不改密无法调业务接口）

---

## 二、我改了哪些接口 service

**本次代理端开发未修改 applications / files 的 service 文件**（`src/api/applications/service.ts` 和 `src/api/files/service.ts` 已包含所有需要接口）。

**之前为打通登录闭环修改：**
- `src/api/auth/service.ts` — 新增 `changePasswordApi()`
- `src/stores/auth.ts` — 适配后端 flat `LoginResponse` 组装 `currentUser`

---

## 三、我用了哪个固定 applicationId 验证

| 场景 | 固定 applicationId | 验证操作 |
|---|---|---|
| 补件上传 | `124` / `142` | 详情页点击「补充材料」，上传新成绩单 |
| 候补确认 | `128` | 点击「接受候补」或「拒绝候补」 |
| 调剂建议 | `130` / `144` | 点击「拒绝调剂」（或接受调剂） |
| 撤销终态 | `132` | 点击「撤销申请」 |
| 新建草稿 | 新建 | 创建后返回列表查看是否出现 |

---

## 四、哪个接口返回和页面需求不匹配

| 问题 | 后端现状 | 前端现状 | 影响 |
|---|---|---|---|
| 附件无法随草稿保存 | `ApplicationDraftRequest` 没有附件字段，数据库无附件关联表 | 创建页已调 `POST /api/files/attachments` 上传附件，但 fileId 不传给后端 | 附件上传成功但无法和申请持久化关联 |
| 补件后状态不推进 | `supplement` 接口只更新 `transcripts.file_id`，不改 `applications.current_status` | 详情页/列表页补件功能已就绪 | 补件后状态仍为 `DOMESTIC_SUPPLEMENT`，需确认是否由国内审核员继续推进 |
| 创建申请 DTO 字段不足 | 只有 `batchId` / `targetSchoolCode` / `targetMajorCode` | 用户决定暂不扩展第四部分 UI | 暂不支持目标国家、第二志愿、是否接收调剂 |

---

## 五、我没有改公共层

- **未修改：** `src/router/index.ts`、`src/api/request.ts`、`src/components/common/*`、`src/layouts/*`
- **修改了：** `src/stores/auth.ts`（登录改密前置依赖，最小化适配，属于越界）

---

## 六、需要负责人决策/支持的事项

1. **是否接受 `stores/auth.ts` 和 `LoginPage.vue` 的越界修改？** 如不接受，需核心负责人将改密闭环迁移到公共层统一维护。
2. **附件字段扩展：** 是否需要在 `ApplicationDraftRequest` 和数据库中增加附件关联？
3. **补件状态推进：** `supplement` 接口是否应自动将状态从 `DOMESTIC_SUPPLEMENT` 推回 `SUBMITTED` 或 `DOMESTIC_REVIEWING`？
