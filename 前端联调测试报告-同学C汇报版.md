# 前端联调测试报告（同学 C 汇报版）

更新时间：2026-04-29

## 1. 本轮目标

本轮工作的目标不是单纯“把页面合并到 `dev`”，而是把当前前端里**登录后最容易暴露的问题**真正收口，并对代理专员、国内审核员、学校专员、管理员四类角色做一轮可复现的联调验证，形成可用于汇报的书面结论。

本轮重点覆盖：

- 登录与角色识别
- 代理专员申请模块
- 国内审核模块
- 学校专员模块
- 候补管理模块
- 管理端基础模块
- 权限边界验证
- 前端构建验证

---

## 2. 本轮修复的前端问题

### 2.1 登录与角色识别

已修复问题：

1. 登录页“学校专员”演示账号错误
   - 原来写成了不存在的 `school01 / school123`
   - 已改成真实账号：
     - `school_usyd_01 / school123`
     - `school_anu_01 / school123`
     - `school_unsw_01 / school123`

2. 前端没有完整适配后端登录返回字段
   - 后端返回的是扁平 camelCase 结构：
     - `userId`
     - `schoolCode`
     - `mustChangePassword`
   - 前端很多地方按旧字段读：
     - `id`
     - `school_code`
     - `must_change_password`
   - 已在鉴权层做统一标准化，保证登录后前端能正确识别当前角色与学校归属。

### 2.2 学校专员模块

已修复问题：

1. 学校审核列表页原先是纯占位页
   - 现已改成“固定联调样本入口页”
   - 学校专员登录后可以直接进入本校可访问样本

2. 学校审核详情页提交条件过松
   - 原先只要是学校审核员就能看到提交按钮
   - 现已收紧为：
     - 当前账号角色为 `SCHOOL_REVIEWER`
     - 且当前申请状态为 `SCHOOL_REVIEWING`

3. 学校专员样本映射错误
   - 原先把不属于当前学校的样本混进了列表
   - 点击后会触发后端校验，返回：
     - `No permission to access this application`
   - 现已按真实学校样本重新映射

### 2.3 候补管理模块

已修复问题：

1. 候补页给了错误样本
   - 原来给了 `130 / 144`，这两个实际是调剂样本
   - 已改成真实候补相关样本：
     - `127 / 128 / 137 / 141`

2. 点击固定样本标签时原先只改输入框，不自动查询
   - 现已改成点击后直接查询

### 2.4 代理专员模块

已修复问题：

1. 申请列表页“搜索框”原先容易让人误以为是全局搜索
   - 但后端并没有接 `keyword`
   - 现已改为“当前页快速筛选”语义

2. 申请列表默认页容量太小，演示时容易显得数据稀少
   - 默认分页从 `10` 提高到 `50`
   - 页面副标题也改成更诚实的描述：
     - 当前账号总记录数
     - 当前页实际显示条数

### 2.5 首页工作台

已修复问题：

1. 首页原先几乎只是说明文案，角色登录后没有明显可走入口
2. 现已改成按角色提供真实快捷入口：
   - 代理专员：新建申请、我的申请、补件样本、候补确认样本
   - 国内审核员：国内审核入口、待认领样本、审核中样本、补件结果样本
   - 学校专员：学校审核入口、候补管理、本校样本池
   - 管理员：批次、学校专业、用户、日志、候补管理

---

## 3. 本轮涉及的主要前端文件

- `admission-system-frontend/src/api/auth/service.ts`
- `admission-system-frontend/src/views/login/LoginPage.vue`
- `admission-system-frontend/src/views/dashboard/DashboardPage.vue`
- `admission-system-frontend/src/views/applications/ApplicationListPage.vue`
- `admission-system-frontend/src/views/domesticreviews/DomesticReviewListPage.vue`
- `admission-system-frontend/src/views/schoolreviews/SchoolReviewListPage.vue`
- `admission-system-frontend/src/views/schoolreviews/SchoolReviewDetailPage.vue`
- `admission-system-frontend/src/views/waitlists/WaitlistManagePage.vue`

---

## 4. 测试环境

### 4.1 服务地址

- 前端：`http://127.0.0.1:5173`
- 后端：`http://127.0.0.1:8080`
- 健康检查：`GET /api/health`

### 4.2 数据库

- 数据库：`admission_system`
- 当前联调样本已存在，可供固定样本页读取

### 4.3 构建验证

已执行：

```bash
npm run build
```

结果：

- `vue-tsc --noEmit` 通过
- `vite build` 通过

结论：本轮前端修改未引入编译错误。

---

## 5. 测试账号

| 角色 | 账号 | 密码 |
| --- | --- | --- |
| 管理员 | `admin` | `admin123` |
| 代理专员 | `agent01` | `agent123` |
| 国内审核员 | `domestic01` | `domestic123` |
| 学校专员（USYD） | `school_usyd_01` | `school123` |
| 学校专员（ANU） | `school_anu_01` | `school123` |
| 学校专员（UNSW） | `school_unsw_01` | `school123` |

---

## 6. 本轮实际执行的测试

说明：

- 本轮优先执行**低副作用测试**
  - 登录
  - 鉴权
  - 只读详情
  - 列表查询
  - 角色权限边界
- 对会推进状态、改变共享样本的动作，没有做大规模批量执行，避免把共享联调库打乱。

### 6.1 环境与认证测试

| 模块 | 测试项 | 结果 | 说明 |
| --- | --- | --- | --- |
| 环境 | `GET /api/health` | 通过 | 返回 `ok` |
| 认证 | `admin` 登录 | 通过 | 返回 `ADMIN` |
| 认证 | `agent01` 登录 | 通过 | 返回 `AGENT` |
| 认证 | `domestic01` 登录 | 通过 | 返回 `DOMESTIC_REVIEWER` |
| 认证 | `school_usyd_01` 登录 | 通过 | 返回 `SCHOOL_REVIEWER` |
| 认证 | `school_anu_01` 登录 | 通过 | 返回 `SCHOOL_REVIEWER` |
| 认证 | `school_unsw_01` 登录 | 通过 | 返回 `SCHOOL_REVIEWER` |
| 认证 | 各角色 `/auth/me` | 通过 | 当前用户信息返回正常 |

### 6.2 代理专员模块测试

执行项：

- `GET /applications/my`
- `GET /applications/124`
- `GET /applications/128`
- `GET /applications/130`

结果：

| 测试项 | 结果 | 说明 |
| --- | --- | --- |
| 我的申请列表 | 通过 | 当前账号数据可读取 |
| 补件样本 124 | 通过 | 详情可读取 |
| 候补确认样本 128 | 通过 | 详情可读取 |
| 调剂建议样本 130 | 通过 | 详情可读取 |

### 6.3 国内审核模块测试

执行项：

- `GET /applications/134`
- `GET /applications/123`
- `GET /applications/124`

结果：

| 测试项 | 结果 | 说明 |
| --- | --- | --- |
| 待认领样本 134 | 通过 | 可读取详情 |
| 审核中样本 123 | 通过 | 可读取详情 |
| 补件结果样本 124 | 通过 | 可读取详情 |

补充说明：

- 国内审核列表页当前是“固定样本入口页”
- 不是后端真实任务池列表
- 但认领、查看、提交这条线已有前端入口和真实接口对接

### 6.4 学校专员模块测试

#### USYD 学校专员

执行项：

- `GET /applications/127`
- `GET /applications/129`
- `GET /applications/141`

结果：全部通过

#### ANU 学校专员

执行项：

- `GET /applications/126`
- `GET /applications/130`
- `GET /applications/137`

结果：全部通过

#### UNSW 学校专员

执行项：

- `GET /applications/128`
- `GET /applications/131`
- `GET /applications/138`
- `GET /applications/144`

结果：全部通过

结论：

- 学校专员现在已经可以按学校归属访问对应样本
- 不再出现“登录后进入错误学校样本导致无权限”的前端映射错误

### 6.5 候补管理模块测试

执行项：

- 管理员访问 `GET /waitlists/128`
- `school_unsw_01` 访问 `GET /waitlists/128`

结果：

| 测试项 | 结果 | 说明 |
| --- | --- | --- |
| 管理员读取候补详情 | 通过 | 返回正常 |
| UNSW 学校专员读取候补详情 | 通过 | 返回正常 |

### 6.6 管理端模块测试

执行项：

- `GET /batches`
- `GET /schools`
- `GET /majors`
- `GET /users`
- `GET /quotas`
- `GET /audit-logs`
- `GET /audit-logs/status-histories?applicationId=128`

结果：

| 测试项 | 结果 | 说明 |
| --- | --- | --- |
| 批次管理数据 | 通过 | 正常返回 |
| 学校数据 | 通过 | 正常返回 |
| 专业数据 | 通过 | 正常返回 |
| 用户管理数据 | 通过 | 正常返回 |
| 配额数据 | 通过 | 正常返回 |
| 审计日志数据 | 通过 | 正常返回 |
| 状态历史数据 | 通过 | 正常返回 |

---

## 7. 权限边界测试

本轮额外执行了反向权限测试，确认系统不是“谁都能看谁的数据”。

| 测试项 | 预期 | 结果 | 说明 |
| --- | --- | --- | --- |
| `school_usyd_01` 访问 `ANU` 申请 `126` | 应失败 | 通过 | 返回 `No permission to access this application` |
| 代理专员访问 `/users` | 应失败 | 通过 | 返回 `403 Forbidden` |
| 国内审核员访问 `/quotas` | 应失败 | 通过 | 返回 `403 Forbidden` |

结论：

- 当前学校归属校验生效
- 管理端权限边界生效
- 角色隔离基本正常

---

## 8. 当前仍然存在的系统限制

这些不是本轮前端修复没做好，而是当前整体系统状态决定的：

### 8.1 国内审核列表不是真任务池

- 当前国内审核页是固定样本入口
- 原因：后端未开放正式国内审核列表接口

### 8.2 学校审核列表不是真任务池

- 当前学校审核页也是固定样本入口
- 原因：后端未开放正式学校审核列表接口

### 8.3 首页没有真实 dashboard 聚合统计

- 当前首页改成了“真实快捷入口页”
- 原因：后端没有提供 `dashboard` 汇总接口

### 8.4 本轮没有批量执行高副作用动作

以下动作会改变共享测试库中的状态，因此没有批量执行：

- 提交申请
- 撤销申请
- 补件上传
- 候补确认接受 / 拒绝
- 国内审核认领 / 提交
- 学校审核提交
- 配额调整
- 用户新增 / 修改

原因：

- 当前测试库为共享联调环境
- 若批量推进状态，可能影响 A、B 及后续答辩演示样本

处理方式：

- 前端入口与接口连通性已验证
- 建议在答辩前由负责人挑一组专用样本做一次受控演示

---

## 9. 本轮结论

### 9.1 已解决的问题

本轮已经解决了以下“前端一操作就暴露”的问题：

- 学校专员演示账号错误
- 登录返回字段与前端用户模型不匹配
- 学校审核列表页纯占位
- 学校专员点击到非本校样本导致无权限
- 学校审核详情页状态不对仍可提交
- 候补管理页固定样本错误
- 代理专员申请列表“假搜索 / 数据显少”观感问题
- 首页工作台只有说明没有真实入口

### 9.2 当前可用于汇报的说法

当前 `dev` 分支前端已经从“能合并但很多角色进去不好用”，修到“各角色都有真实可走入口，并完成了一轮按角色的联调验证”。

更准确的汇报口径是：

1. 代理专员主链路可读可进，页面交互已比合并前稳定很多
2. 国内审核已具备固定样本入口与详情链路
3. 学校专员已能按学校归属访问正确样本，不再出现演示账号和无权限乱跳问题
4. 管理端基础查询接口联通正常
5. 当前剩余限制主要来自后端尚未提供的正式任务池列表与 dashboard 汇总接口

---

## 10. 建议下一步

如果后续还要进一步提升汇报效果，优先级建议如下：

1. 增加“专用答辩样本”
   - 避免共享联调样本被多次操作后状态漂移

2. 为国内审核和学校审核补正式任务池接口
   - 这样列表页就不再是固定样本入口

3. 为首页补 dashboard 汇总接口
   - 这样首页可以从“快捷入口页”升级成真正工作台

4. 在答辩前做一次受控演示彩排
   - 选择一组固定样本
   - 按代理 -> 国内审核 -> 学校审核 -> 候补/调剂/关闭的路径完整走一遍

