还需要，而且这些规定比“分工”本身更重要。
**真正让三个人不冲突的，不是只分模块，而是你先把“公共约束”定死。**

你现在已经有：

+ 分工方案
+ 后端接口文档 v4
+ PRD 正式版
+ 数据库设计与表映射

接下来你要做的是：**先完成一套“开工前冻结清单”**，然后再让另外两个人开始。

------

# 一、除了分工，还必须规定什么

## 1. 冻结“唯一真相源”

你们三个人必须约定，后续争议以哪份文档为准：

优先级建议：

1. **PRD**
2. **接口文档 v4**
3. **数据库设计文档**
4. 群里补充规则

原因是：

+ PRD 定业务边界、角色权限、状态机、候补/调剂/配额原则
+ 接口文档 v4 定当前真实开发的状态流转和接口边界
+ 数据库设计文档定表结构、关系和接口读写表映射

### 你要发群里一句话

“业务冲突先看 PRD，接口怎么做看接口文档，表怎么落看数据库设计；个人理解不算规则。”

------

## 2. 冻结状态枚举和状态流转

这条必须单独强调，因为这是最大冲突源。

接口文档已经定了：

+ application 状态只能通过指定业务接口变更
+ 学校审核只保留一个 submit
+ 候补补位后进入 `WAITLIST_PENDING_CONFIRM`
+ 国内审核通过自动进学校审核中
+ 接受调剂后原申请立即关闭

### 你要规定

+ 任何人不得新增状态
+ 任何人不得改状态名称
+ 任何人不得前端本地判断并伪造状态流转
+ 任何人不得直接 update `applications.current_status`

数据库设计也已经明确：
`applications.current_status` 只能走 service，不允许直接改。

------

## 3. 冻结接口返回结构

你们最容易打架的还有一件事：返回字段不统一。

接口文档已经统一了：

+ 通用 `code / message / data`
+ 分页结构
+ 申请详情的聚合返回块结构

### 你要规定

+ A/B 不允许自己改响应格式
+ 不允许出现一部分接口返回 `success: true`，另一部分返回 `code: 0`
+ 不允许列表字段名乱写，例如有人叫 `items`，有人叫 `list`

------

## 4. 冻结数据库“归属权”

数据库设计已经把表的归属基本分清了：
你主控核心表，A 主要触达学生/成绩/材料/申请草稿，B 主要触达国内审核表。

### 你要规定

#### 只允许你改的表

+ `applications`
+ `rule_snapshots`
+ `school_reviews`
+ `school_quotas`
+ `major_quotas`
+ `waitlist_records`
+ `application_status_histories`
+ `audit_logs`
+ `admission_batches`
+ `schools`
+ `majors`
+ `users`

#### A 只能改

+ `students`
+ `transcripts`
+ `personal_statements`
+ `applications` 里仅草稿相关字段，不碰核心状态

#### B 只能改

+ `domestic_reviews`
+ 通过你的审核接口推动申请状态，不直接写状态字段

------

## 5. 冻结目录结构和文件归属

如果不定目录，三个人即使功能不重叠，也会改同一个文件。

### 你要先规定

后端目录例如：

```text
src/
  modules/
    auth/
    users/
    batches/
    schools/
    majors/
    quotas/
    files/
    applications/
    domesticreviews/
    schoolreviews/
    waitlists/
    adjustments/
    auditlogs/
  common/
    enums/
    constants/
    exceptions/
    guards/
    interceptors/
    utils/
```

### 文件归属必须写死

+ `common/enums/**` 只有你改
+ `applications/**` 核心状态 service 只有你改
+ `schoolreviews/**` 只有你改
+ `waitlists/**` 只有你改
+ `domesticreviews/**` B 改
+ `files/**`、申请草稿/详情查询这类 A 改
+ 路由注册总入口只有你改

------

## 6. 冻结“谁不能碰什么”

这条要明文写，不然默认就是谁都能改。

分工文档已经写了：

+ A 不写审核
+ B 不写申请
+ 两人都不碰配额
+ A/B 不碰 application 核心状态接口

你要把它变成“禁止项”：

### A 禁止

+ 改状态枚举
+ 改状态流转逻辑
+ 改学校审核接口
+ 改 quota 表
+ 改日志机制

### B 禁止

+ 改申请草稿逻辑
+ 改学校审核
+ 改候补/调剂
+ 改 quota 逻辑
+ 改 application 核心状态接口

------

## 7. 冻结代码风格和命名

这个看起来小，实际上能减少很多无意义 PR 冲突。

### 最少定这几项

+ 统一语言：变量和表字段统一英文
+ 状态值统一全大写下划线
+ DTO 命名统一：`CreateXxxRequest / UpdateXxxRequest`
+ Service 命名统一：`XxxService`
+ Controller 命名统一：`XxxController`
+ SQL 字段统一蛇形命名 or 驼峰映射规则先定死
+ 统一格式化工具和 lint 规则

否则三个人会在“改功能”的 PR 里顺手重排代码，冲突会暴增。

------

## 8. 冻结 PR 规则

分工文档里已经建议“一个 PR 只做一件事”。
你要再补几条：

### 必须规定

+ 一个分支只做一个模块
+ 一个 PR 不超过一个业务点
+ PR 标题格式统一
+ PR 描述必须写“改了哪些接口 / 哪些表 / 是否改状态流转”
+ 改核心表必须 @你 review
+ 改公共文件必须 @你 review

### 你可以直接规定

+ 没文档更新的不合并
+ 没自测说明的不合并
+ 改了接口但没同步接口文档的不合并

------

## 9. 冻结测试边界

PRD 已经给了测试重点：

+ 创建申请
+ 提交申请
+ 国内审核
+ 学校审核
+ 调剂派生申请
+ 候补补位
+ 撤销申请
+ 最后一个名额并发占位
+ 候补超时未确认
+ 同一学生重复申请
+ 撤销后重申同校
+ 批次结束候补失效
+ 权限越权访问等

### 你要规定

每个人提交 PR 时都要附：

+ 自测范围
+ 测试数据
+ 正常流程截图/接口结果
+ 至少 1 个异常场景

------

## 10. 冻结“改需求流程”

这是最容易被忽略的。

开发中如果有人说：

+ 我觉得这个字段应该加
+ 我觉得这个状态更合理
+ 我觉得这个接口返回再多一点

如果没有流程，项目马上乱。

### 你要规定

所有新增或修改，必须经过你：

+ 改 PRD 规则：你批准
+ 改接口：你批准
+ 改表结构：你批准
+ 改状态流转：你批准
+ 改配额/候补/调剂逻辑：只能你改

------

# 二、你先要做好什么，再让另外两人开始

这个最关键。我按顺序给你列。

------

## 第一阶段：你必须先做完的“开工前必备项”

这些没做完，先别让他们动。

### 1. 最终冻结文档包

你先整理成 4 份成品：

1. PRD
2. 分工与禁止项
3. 接口文档 v4
4. 数据库设计文档

这 4 份就是你们的“开工基线”。
目前这些内容你已经基本有了。

------

### 2. 搭项目骨架

你先把这些提交到 `dev`：

+ 登录/鉴权基础
+ 用户角色枚举
+ 通用响应结构
+ 路由骨架
+ 公共异常处理
+ 公共工具类
+ 状态枚举
+ 基础目录结构
+ application 状态机骨架
+ school-review 骨架
+ domestic-review 骨架
+ files 骨架

这是分工文档里“你必须先做的项目骨架”。

------

### 3. 建数据库并跑通迁移

你至少要先把核心表建起来：

+ `users`
+ `admission_batches`
+ `schools`
+ `majors`
+ `school_quotas`
+ `major_quotas`
+ `students`
+ `transcripts`
+ `personal_statements`
+ `applications`
+ `rule_snapshots`
+ `domestic_reviews`
+ `school_reviews`
+ `waitlist_records`
+ `application_status_histories`
+ `audit_logs`

然后准备一份初始化数据：

+ 学校
+ 专业
+ 批次
+ 用户账号
+ 基础配额

没有这些，A 和 B 跑不起来。

------

### 4. 先把核心接口做通

你必须先实现这些，因为 A/B 都依赖它们：

+ 登录
+ 获取当前用户
+ 创建申请草稿
+ 编辑草稿
+ 提交申请
+ 国内审核接单
+ 国内审核提交
+ 学校审核提交（骨架可先有）
+ 查询申请详情
+ 文件上传骨架

这是接口文档里最关键的一批基础接口。

------

### 5. 先准备测试账号和样例数据

你要给他们直接发：

+ 代理账号
+ 国内审核员账号
+ 管理员账号
+ 1 个批次
+ 2~3 所学校
+ 对应专业
+ 基础 quota 数据

否则他们一开始就会被“没数据可测”卡住。

------

## 第二阶段：你给另外两个人的“开工包”

你准备好后，再让他们开始。

### 给 A 的包

+ 接口文档中 A 负责的接口列表
+ 草稿创建/编辑/查询/补件的 DTO 示例
+ 文件上传说明
+ 申请详情返回结构示例
+ 测试账号 + 测试数据
+ 明确禁止改状态逻辑和核心 application 字段

### 给 B 的包

+ 国内审核模块接口列表
+ 国内审核请求体示例
+ 审核详情返回结构示例
+ 测试账号 + 测试数据
+ 明确禁止改学校审核、配额、调剂、候补逻辑

------

## 第三阶段：他们开始做的时候，你还要盯什么

### 1. 每人先只开一个分支

+ A：`feature/agent`
+ B：`feature/domestic-review`
+ 你：`feature/core`

### 2. 每人第一次 PR 只做骨架

不要一上来做大功能。

A 第一个 PR：

+ 申请列表页骨架 / 草稿创建页骨架

B 第一个 PR：

+ 国内审核列表页骨架 / 详情页骨架

你：

+ 核心状态接口和学校审核骨架

这样最稳。

### 3. 每天只允许合并小 PR

不要等他们堆到很大再合。

------

# 三、你可以直接发群的“开工规则”

你可以直接复制这段：

> 1. 业务以 PRD 为准，接口以接口文档为准，表结构以数据库设计文档为准。
> 2. application 状态只能走既定接口，不允许直接改数据库状态。
> 3. A 只做申请端，B 只做国内审核，我负责核心状态、学校审核、配额、候补、调剂、日志。
> 4. 不允许新增状态、不允许改公共响应格式、不允许改公共枚举。
> 5. 改公共文件、核心表、状态流转、配额逻辑，必须先和我确认。
> 6. 一个 PR 只做一个模块或一个功能点，PR 必须写清楚改了哪些接口、哪些表。
> 7. 没有自测说明、没有文档同步更新的 PR 不合并。
> 8. 开发中任何需求变更，先提出来，我统一决定是否改文档和表结构。

------

# 四、最简执行顺序

## 你现在立刻要做

1. 把 4 份文档定稿
2. 把项目骨架和数据库建好
3. 把核心接口和基础数据跑通
4. 发测试账号和分工清单

## 然后再让他们开始

+ A 做申请端
+ B 做国内审核
+ 你继续做学校审核 + 配额 + 候补 + 调剂

------

# 五、最后一句话

**你先不要急着让他们“开始写功能”，先把“规则、骨架、接口、表、测试数据”铺好。**
这一步做好了，后面基本是“按轨道跑”；这一步没做好，三个人再怎么分工也会撞。依据你现有的分工文档、接口文档、数据库设计和 PRD，这就是最稳的启动方式。

我可以下一条直接给你整理成一份 **《开工前检查清单 + 发给组员的任务说明》**。



在 A 和 B 开工前，你先完成：

1. 项目骨架初始化
2. 数据库表建好并初始化基础数据
3. 登录 / 获取当前用户跑通
4. 通用响应结构写好
5. JWT 鉴权写好
6. 状态枚举和公共常量写好
7. 申请核心接口骨架写好
8. 学校审核 submit 接口骨架写好
9. 文件上传骨架写好
10. 测试账号准备好
