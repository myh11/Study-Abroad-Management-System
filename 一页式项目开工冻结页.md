# 一页式项目开工冻结页

文档优先级：`PRD > 后端接口文档 v4 > 数据库与联调协作说明 > 技术路线说明 > 个人理解`

## 一、当前系统版本信息

- 后端冻结版本：按《后端接口文档 v4（按当前代码实现修正版）》执行。
- 当前后端技术基线：`Java 17 + Spring Boot 3.x + Spring Security + JWT + MyBatis-Plus + MySQL 8.4`
- 当前已实现模块：
  - 真实登录 / `JWT` / `RBAC`
  - 首次登录强制改密 / 连续输错锁定
  - 申请草稿创建、编辑、我的申请、详情、补件
  - 申请命令流：提交、撤销、候补确认、接受调剂、拒绝调剂、关闭
  - 国内审核：认领、提交
  - 学校审核：唯一 `submit` 接口
  - 候补查询 / `promote` / `invalidate` / 超时自动关闭 / 批次结束自动关闭
  - 文件上传、预览、下载
  - 管理端：批次、学校、专业、配额、用户、日志查询
- 当前未实现模块：
  - `/api/dashboard/**`
  - `/api/domesticreviews/pending`
  - `/api/domesticreviews/{id}`
  - `/api/domesticreviews/records`
  - `/api/domesticreviews/records/{reviewId}`
  - `/api/school-reviews/pending`
  - `/api/school-reviews/{id}`
  - `/api/school-reviews/records`
  - `/api/school-reviews/records/{reviewId}`
  - `/api/applications/{id}/status-history`
  - 批次 `start / finish` 独立接口
  - 用户 `reset-password`

## 二、当前接口基线（唯一可信）

- API 统一前缀：`/api`
- 通用返回结构：
  - 成功：`{ "code": 0, "message": "success", "data": ... }`
  - 失败：`{ "code": 1, "message": "错误信息", "data": null }`
  - 分页：`data` 内固定包含 `list / page / pageSize / total`
- 当前有效接口范围：只允许使用《后端接口文档 v4》中已实现接口；任何群聊、旧稿、截图、历史讨论中的接口一律无效。
- 认证基线：除登录外，所有敏感接口必须携带 `Authorization: Bearer <token>`；首次登录且 `mustChangePassword=true` 时，仅允许访问 `/api/auth/me` 和 `/api/auth/change-password`。

## 三、当前数据库运行配置

- 数据库连接基线：
  - Host：`localhost`
  - Port：`3306`
  - DB：`admission_system`
  - User：`root`
  - Password：`mYh031005`
- 服务端口：`8080`
- 配置方式：`application.yml` 使用“环境变量 + 默认值”模式，统一读取 `SERVER_PORT / DB_HOST / DB_PORT / DB_NAME / DB_USERNAME / DB_PASSWORD`；本地与 CI 使用同一套键。
- 初始化方式：只允许通过 `schema.sql` 和 `data.sql` 初始化数据库。
- 直接改库规则：不允许直接改运行库关键业务字段；状态、配额、候补、审核结果只能通过后端接口或业务 service 修改。

## 四、测试账号（联调统一使用）

| 账号 | 密码 | 角色 | 归属学校 | 强制说明 |
| --- | --- | --- | --- | --- |
| `admin` | `admin123` | `ADMIN` | 无 | 首次登录必须先改密 |
| `agent01` | `agent123` | `AGENT` | 无 | 只可操作自己的申请 |
| `domestic01` | `domestic123` | `DOMESTIC_REVIEWER` | 无 | 只走国内审核接口 |
| `school_usyd_01` | `school123` | `SCHOOL_REVIEWER` | `USYD` | 只能处理 `USYD` 数据 |
| `school_anu_01` | `school123` | `SCHOOL_REVIEWER` | `ANU` | 只能处理 `ANU` 数据 |
| `school_unsw_01` | `school123` | `SCHOOL_REVIEWER` | `UNSW` | 只能处理 `UNSW` 数据 |

- 所有初始化账号默认 `must_change_password = true`。
- 首次登录后，必须立即调用 `POST /api/auth/change-password`，否则不得继续联调业务接口。

## 五、固定联调数据（必须用）

| applicationId | 当前状态 | 只用于 |
| --- | --- | --- |
| `124` | `DOMESTIC_SUPPLEMENT` | 补件上传 + 重新提交 |
| `127` | `WAITLISTED` | 候补详情 + 学校推进候补 |
| `128` | `WAITLIST_PENDING_CONFIRM` | 候补确认接受/拒绝 |
| `129` | `RESERVED` | 预录取终态展示 |
| `130` | `ADJUSTMENT_SUGGESTED` | 调剂建议查看 + 拒绝调剂 |
| `132` | `CANCELED` | 撤销终态展示 + 同批次不可再申同校验证 |
| `137` | `WAITLISTED` | 第二组候补并行联调 |
| `138` | `RESERVED` | 第二组预录取展示 |
| `142` | `DOMESTIC_SUPPLEMENT` | 第二组补件联调 |
| `144` | `ADJUSTMENT_SUGGESTED` | 第二组调剂建议联调 |

- 候补推进链路只允许使用 `127 / 137 / 128`。
- 补件链路只允许使用 `124 / 142`。
- 调剂链路优先使用 `130 / 144`；如需验证“接受调剂生成新草稿”，必须通过学校审核接口重新制造，不允许手改库。

## 六、当前模块完成度

- 已完成模块：
  - 认证与权限
  - 申请端核心流
  - 国内审核核心流
  - 学校审核核心动作
  - 候补闭环
  - 文件上传
  - 管理端配置
  - 审计日志查询
- 未完成模块：
  - 工作台 `dashboard`
  - 国内审核列表/详情/记录查询接口
  - 学校审核列表/详情/记录查询接口
  - 申请状态历史查询接口
  - 批次独立 `start / finish` 接口
  - 用户重置密码接口

## 七、强制规则（必须执行）

- 申请状态只能通过后端接口修改，不允许任何人直接改 `applications.current_status`。
- 不允许直接修改以下字段：`applications.current_status`、`applications.cancel_reason`、`applications.close_reason`、`applications.rule_snapshot_id`、`applications.waitlist_confirm_deadline`、`applications.reserved_at`、`applications.canceled_at`、`applications.closed_at`。
- 不允许直接修改以下候补字段：`waitlist_records.waitlist_status`、`waitlist_records.promoted_at`、`waitlist_records.confirm_deadline`、`waitlist_records.expired_at`。
- 不允许直接修改配额字段：`school_quotas.used_quota / remaining_quota`、`major_quotas.used_quota / remaining_quota`。
- 联调必须优先使用本页固定账号和固定 `applicationId`；未经负责人确认，不允许各自新造脏数据。
- 业务规则冲突时，先看 `PRD`；接口可用性冲突时，只认《后端接口文档 v4》；数据库操作冲突时，只认《数据库与联调协作说明》。
