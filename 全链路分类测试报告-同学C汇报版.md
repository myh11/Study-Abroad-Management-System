# 全链路分类测试报告（同学 C 汇报版）

更新时间：2026-04-29

## 1. 本轮工作目标

本轮目标不是只修前端页面，而是先补齐当前系统里真正影响可用性的后端缺口，再做一轮按模块、按角色、按权限边界的**全链路分类测试**，形成可用于汇报的书面结果。

本轮重点分两步：

1. 修复后端缺口  
2. 进行分类测试，并沉淀测试报告

---

## 2. 本轮后端修复内容

### 2.1 新增 Dashboard 汇总接口

新增接口：

- `GET /api/dashboard/summary`

作用：

- 根据当前登录角色返回对应统计卡片
- 不再需要前端首页自己硬写说明页或固定文案

当前支持：

- 管理员：申请总数、用户总数、活跃候补、批次数
- 代理专员：我的申请总数、草稿数、待补件、候补待确认
- 国内审核员：待认领、审核中、补件结果、拒绝结果
- 学校专员：学校审核中、候补中、预录取、调剂建议

### 2.2 新增国内审核正式列表接口

新增接口：

- `GET /api/domesticreviews`

作用：

- 返回国内审核正式分页列表
- 不再只能靠固定样本硬编码做前端入口

默认覆盖状态：

- `SUBMITTED`
- `DOMESTIC_REVIEWING`
- `DOMESTIC_SUPPLEMENT`
- `DOMESTIC_REJECTED`

支持筛选参数：

- `page`
- `pageSize`
- `status`
- `batchId`

### 2.3 新增学校审核正式列表接口

新增接口：

- `GET /api/school-reviews`

作用：

- 返回学校审核正式分页列表
- 学校专员按当前学校自动筛选
- 管理员可通过参数查看指定学校列表

默认覆盖状态：

- `SCHOOL_REVIEWING`
- `WAITLISTED`
- `WAITLIST_PENDING_CONFIRM`
- `RESERVED`
- `ADJUSTMENT_SUGGESTED`
- `SCHOOL_REJECTED`
- `CLOSED`

支持筛选参数：

- `page`
- `pageSize`
- `status`
- `batchId`
- `schoolCode`

### 2.4 调整后端安全配置

本轮不是简单放宽权限，而是做了**读取与动作分离**：

- `GET /api/dashboard/**`：登录即可访问
- `GET /api/domesticreviews/**`：允许 `DOMESTIC_REVIEWER`、`ADMIN`
- `POST /api/domesticreviews/**`：仍限制为 `DOMESTIC_REVIEWER`
- `GET /api/school-reviews/**`：允许 `SCHOOL_REVIEWER`、`ADMIN`
- `POST /api/school-reviews/**`：仍限制为 `SCHOOL_REVIEWER`

这样做的好处是：

- 管理员可以查看审核列表用于排查与演示
- 但不会误开放真正的审核提交权限

### 2.5 修复后端测试工程问题

修复内容：

- 修正 `ApplicationCommandServiceTest` 中构造器参数滞后问题

验证结果：

- `mvn -q test-compile` 已通过

说明：

- 之前这个测试类参数没跟生产代码同步，会卡住部分 Maven 流程
- 已同步修复

---

## 3. 本轮前端联动调整

虽然本轮重点是后端，但为了让新增后端能力真正被页面用到，也做了必要的前端联动：

1. 首页工作台改为读取 `dashboard` 汇总接口
2. 国内审核列表页改为读取 `/api/domesticreviews`
3. 学校审核列表页改为读取 `/api/school-reviews`
4. 登录页学校专员演示账号改成真实账号
5. 学校专员当前学校识别继续走标准化用户字段

验证结果：

- 前端 `npm run build` 通过

---

## 4. 构建与运行环境验证

### 4.1 后端构建验证

已执行：

```bash
mvn -q -DskipTests compile
mvn -q test-compile
```

结果：

- 主代码编译通过
- 测试源码编译通过

### 4.2 后端运行验证

由于 `spring-boot:run` 在当前环境下存在流程不稳定问题，本轮采用了更稳的方式：

1. `mvn package -Dmaven.test.skip=true`
2. `java -jar target/admission-system-backend-0.0.1-SNAPSHOT.jar --spring.sql.init.mode=never`

结果：

- 服务正常启动
- `GET /api/health` 正常返回

### 4.3 前端构建验证

已执行：

```bash
npm run build
```

结果：

- `vue-tsc --noEmit` 通过
- `vite build` 通过

---

## 5. 测试账号

| 角色 | 账号 | 密码 |
| --- | --- | --- |
| 管理员 | `admin` | `admin123` |
| 代理专员 | `agent01` | `agent123` |
| 国内审核员 | `domestic01` | `domestic123` |
| 学校专员（USYD） | `school_usyd_01` | `school123` |
| 学校专员（ANU） | `school_anu_01` | `school123` |
| 学校专员（UNSW） | `school_unsw_01` | `school123` |

---

## 6. 分类测试范围

本轮测试按以下 9 类执行：

1. 环境健康检查
2. 登录与鉴权
3. Dashboard 汇总
4. 代理专员模块
5. 国内审核模块
6. 学校审核模块
7. 候补管理模块
8. 管理端基础模块
9. 权限边界

此外单独补做了：

10. 一条**新增样本的真实业务闭环测试**

---

## 7. 分类测试结果

### 7.1 环境健康检查

| 测试项 | 结果 | 说明 |
| --- | --- | --- |
| `GET /api/health` | 通过 | 返回 `ok` |

### 7.2 登录与鉴权

| 测试项 | 结果 | 说明 |
| --- | --- | --- |
| `admin` 登录 | 通过 | 返回 `ADMIN` |
| `agent01` 登录 | 通过 | 返回 `AGENT` |
| `domestic01` 登录 | 通过 | 返回 `DOMESTIC_REVIEWER` |
| `school_usyd_01` 登录 | 通过 | 返回 `SCHOOL_REVIEWER` |
| `school_anu_01` 登录 | 通过 | 返回 `SCHOOL_REVIEWER` |
| `school_unsw_01` 登录 | 通过 | 返回 `SCHOOL_REVIEWER` |
| 各角色 `/auth/me` | 通过 | 当前用户信息返回正常 |

### 7.3 Dashboard 汇总测试

| 测试项 | 结果 | 说明 |
| --- | --- | --- |
| `ADMIN /dashboard/summary` | 通过 | 返回 4 张统计卡 |
| `AGENT /dashboard/summary` | 通过 | 返回 4 张统计卡 |
| `DOMESTIC /dashboard/summary` | 通过 | 返回 4 张统计卡 |
| `SCHOOL_USYD /dashboard/summary` | 通过 | 返回 4 张统计卡 |
| `SCHOOL_ANU /dashboard/summary` | 通过 | 返回 4 张统计卡 |
| `SCHOOL_UNSW /dashboard/summary` | 通过 | 返回 4 张统计卡 |

结论：

- 首页工作台已经有真实后端汇总支撑
- 不再是纯说明占位

### 7.4 代理专员模块测试

已测：

- `GET /applications/my?page=1&pageSize=20`
- 新建草稿
- 提交申请

结果：

| 测试项 | 结果 | 说明 |
| --- | --- | --- |
| 我的申请列表 | 通过 | 当前账号总数返回正常 |
| 创建草稿 | 通过 | 成功创建新申请 `145` |
| 提交申请 | 通过 | 新申请可正常从 `DRAFT` 提交到 `SUBMITTED` |

### 7.5 国内审核模块测试

已测：

- `GET /domesticreviews`
- 新申请认领
- 新申请提交国内审核 `PASS`

结果：

| 测试项 | 结果 | 说明 |
| --- | --- | --- |
| 正式列表接口 `/domesticreviews` | 通过 | 当前返回 `total=7` |
| 认领新申请 | 通过 | 新申请 `145` 成功认领 |
| 提交国内审核 `PASS` | 通过 | 新申请状态成功推进到学校审核 |

结论：

- 国内审核不再只有固定样本入口
- 正式列表与动作接口都已打通

### 7.6 学校审核模块测试

已测：

- `GET /school-reviews`
- 管理员按学校筛选学校审核列表
- 新申请提交学校审核 `REJECT`

结果：

| 测试项 | 结果 | 说明 |
| --- | --- | --- |
| `school_usyd_01` 正式列表接口 `/school-reviews` | 通过 | 当前返回 `total=6` |
| 管理员按 `schoolCode=ANU` 查看学校审核列表 | 通过 | 当前返回 `total=4` |
| `school_anu_01` 提交学校审核 `REJECT` | 通过 | 新申请 `145` 成功推进到学校拒绝 |

结论：

- 学校审核已有正式列表接口
- 学校专员可直接查当前学校申请
- 管理员也可用于排查和演示

### 7.7 候补管理模块测试

已测：

- `GET /waitlists/128`

结果：

| 测试项 | 结果 | 说明 |
| --- | --- | --- |
| `school_unsw_01` 读取候补详情 `128` | 通过 | 返回正常 |

说明：

- 本轮重点验证了候补只读链路
- 未批量执行候补推进、手动失效等高副作用操作

### 7.8 管理端基础模块测试

已测：

- `GET /batches`
- `GET /schools`
- `GET /majors`
- `GET /users`
- `GET /quotas`
- `GET /audit-logs`
- `GET /audit-logs/status-histories?applicationId=128`

结果：

| 测试项 | 结果 | 说明 |
| --- | --- | --- |
| 批次管理数据 | 通过 | 正常返回 |
| 学校数据 | 通过 | 正常返回 |
| 专业数据 | 通过 | 正常返回 |
| 用户管理数据 | 通过 | 正常返回 |
| 配额数据 | 通过 | 正常返回 |
| 审计日志数据 | 通过 | 正常返回 |
| 状态历史数据 | 通过 | 正常返回 |

### 7.9 权限边界测试

本轮额外验证了“不该进的地方确实会被拦住”。

| 测试项 | 预期 | 结果 | 说明 |
| --- | --- | --- | --- |
| `school_usyd_01` 访问 `ANU` 申请 `126` | 应失败 | 通过 | 返回 `No permission to access this application` |
| 代理专员访问 `/users` | 应失败 | 通过 | 返回 `403 Forbidden` |
| 国内审核员访问 `/quotas` | 应失败 | 通过 | 返回 `403 Forbidden` |

结论：

- 学校归属权限生效
- 管理端权限生效
- 角色边界控制正常

---

## 8. 真实新增样本闭环测试

为了避免只测试“现成只读样本”，本轮专门新建了一条独立样本，走完整业务链路。

### 8.1 新样本信息

- 新建申请 ID：`145`
- 目标学校：`ANU`
- 目标专业：`ANU_CS`
- 创建方式：代理专员真实接口创建

### 8.2 实际流转路径

1. 代理专员创建草稿
2. 代理专员提交申请
3. 国内审核员认领
4. 国内审核员提交审核 `PASS`
5. 学校专员（ANU）提交学校审核 `REJECT`

### 8.3 最终结果

| 测试项 | 结果 | 说明 |
| --- | --- | --- |
| 新申请最终状态 | 通过 | 最终状态为 `SCHOOL_REJECTED` |

结论：

- 一条真实新增申请已经完成从代理到学校审核的闭环验证
- 说明本轮不是只验证静态读取，而是验证了真实状态推进

---

## 9. 当前仍未完全覆盖的测试项

虽然本轮已经是分类全面测试，但仍有一些高副作用或高成本项没有批量执行，需要在汇报里如实说明：

### 9.1 未批量执行的高副作用动作

- 候补补位 `promote`
- 候补手动失效 `invalidate`
- 调剂接受 / 拒绝
- 配额调整
- 用户新增 / 修改
- 文件真实上传

### 9.2 未执行原因

主要原因有两类：

1. 这些动作会明显改变共享联调库状态  
2. 当前仓库里没有现成的稳定二进制测试文件资产（例如专用 PDF）用于重复文件上传测试

### 9.3 当前处理方式

- 已优先完成读接口、列表、鉴权、状态推进主链路和一条新增样本闭环
- 对剩余高副作用动作，建议在答辩前用**专用答辩样本**单独做一次受控彩排

---

## 10. 本轮总评结论

### 10.1 已完成的关键改善

本轮已经把系统从“前端能打开，但后端缺正式接口支撑”的状态，推进到：

1. 首页已有真实 dashboard 汇总接口
2. 国内审核已有正式列表接口
3. 学校审核已有正式列表接口
4. 后端测试工程中已知的一个阻塞项已修复
5. 各角色登录、列表、详情、权限边界验证通过
6. 已完成一条真实新增申请的业务闭环测试

### 10.2 现在可以怎么汇报

比较准确、也比较稳妥的汇报口径是：

> 本轮不是只把分支合并到 `dev`，而是继续补齐了后端缺失的正式能力，包括 dashboard 汇总、国内审核列表、学校审核列表，并完成了一轮按环境、认证、角色模块、权限边界、业务闭环分类执行的全链路测试。当前系统已经不再只是“页面能打开”，而是主要角色都有真实接口支撑，且已验证关键状态流转。

### 10.3 当前系统状态判断

当前版本可以定义为：

**“具备真实后端支撑、已完成分类联调验证的集成测试版本”**

它已经明显强于之前的“仅能合并、局部能演示”的状态。

---

## 11. 建议后续动作

1. 将本轮前后端修复统一提交到 `dev`
2. 为答辩单独准备专用样本，避免共享联调库被临时操作打乱
3. 额外做一次受控彩排，重点演示：
   - 代理创建与提交
   - 国内审核认领与提交
   - 学校审核结果提交
   - 候补只读查看
   - 管理端日志与配额查看

